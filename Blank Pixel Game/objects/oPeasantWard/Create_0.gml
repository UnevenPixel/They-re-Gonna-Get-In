// Inherit oTrainingBuildingParent -> oBuildingParent: team/radius, then
// BuildingApplyDefinition (training state) and the Step-event queue tick
// (TrainingUpdateQueue) -- all from this building's registered
// BuildingDefinition. Nothing else to set here.
event_inherited();
